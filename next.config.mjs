/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: '1540257937.rsc.cdn77.org',
            },
        ],
    },
};

export default nextConfig;
